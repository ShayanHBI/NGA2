# Add appropriate YAML interface libraries
ifdef YAML_DIR
  LIBRARY_LOCATIONS += $(YAML_DIR)/build/src
  LIBRARIES += -lfortran-yaml-c -llibyaml_interface
  INCLUDE_LOCATIONS += $(YAML_DIR)/build/modules
else
  $(error YAML_DIR not defined, but it is currently required by NGA2)
endif
